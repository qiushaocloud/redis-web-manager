stages:
  - sync-code
  - build
  - deploy

sync-code:
  stage: sync-code
  only:
    - master
  script:
    - bash push-remote-git-repos.sh
  tags:
    - qiushaocloud-runner

build:
  stage: build
  only:
    - master
  script:
    - docker build -t qiushaocloud/redis-web-manager:latest .
  tags:
    - qiushaocloud-runner

deploy:
  stage: deploy
  only:
    - master
  script:
    - docker login --username $DOCKER_HUB_USER_NAME --password $DOCKER_HUB_USER_PASSWORD
    - docker push qiushaocloud/redis-web-manager:latest
  tags:
    - qiushaocloud-runner

build-tags:
  stage: build
  only:
    - tags
  script:
    - docker build -t qiushaocloud/redis-web-manager:latest .
    - docker tag qiushaocloud/redis-web-manager:latest qiushaocloud/redis-web-manager:$CI_COMMIT_REF_NAME
  tags:
    - qiushaocloud-runner

deploy-tags:
  stage: deploy
  only:
    - tags
  script:
    - docker login --username $DOCKER_HUB_USER_NAME --password $DOCKER_HUB_USER_PASSWORD
    - docker push qiushaocloud/redis-web-manager:latest
    - docker push qiushaocloud/redis-web-manager:$CI_COMMIT_REF_NAME
  tags:
    - qiushaocloud-runner
